import React, { ReactElement } from 'react';

const App: React.FunctionComponent = (): ReactElement => {
    return (
        <div>
            <h2>Let's Go</h2>
        </div>
    );
}

export default App;
